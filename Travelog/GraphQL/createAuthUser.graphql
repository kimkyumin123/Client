mutation createAuthUser(
  $nickName: String!
  $avatar: String
  $bio: String
  $platformType: String!
  $token: String!
) {
  createAuthUser(
    nickName: $nickName
    avatar: $avatar
    bio: $bio
    platformType: $platformType
    token: $token
  ) {
    ok
    error
  }
}
